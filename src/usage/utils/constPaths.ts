const BASE = "/dev"

export const AuthPaths = {
    LOGIN: BASE + "/auth/login",
    REG: BASE + "/auth/registration",
    TOKENS: BASE + "/auth/tokens",
    REFRESH: BASE + "/auth/refresh"
}

export const PostPaths = {
    POST: BASE + "/post"
}

export const UsersPaths = {
    USERS: BASE + "/users"
}
